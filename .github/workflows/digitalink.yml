name: Build Digital Ink

on:
  push:
    paths:
      - 'android/digitalink/**'
      - '.github/workflows/digitalink.yml'
  pull_request:
    paths:
      - 'android/digitalink/**'

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: android

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Grant execute permission to Gradle
        run: chmod +x ./gradlew

      - name: Build Digital Ink module
        run: ./gradlew :digitalink:assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: digitalink-debug-apk
          path: android/digitalink/build/outputs/apk/debug/digitalink-debug.apk
